export type Model = {
  _id: string;
  createdAt: string;
  updatedAt: string;
};

export type Image = {
  size: number;
  path: string;
};

export type Location = {
  latitude: number;
  longitude: number;
};
